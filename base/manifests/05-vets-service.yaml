---
# ============================================================================
# Vets Service Rollout (Blue-Green 배포)
# ============================================================================
# Argo Rollouts를 사용한 Blue-Green 배포 전략
# - 신버전 배포 시 Preview Service로 테스트
# - 검증 후 Active Service로 전환
# - 문제 시 즉시 롤백 가능
# ============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: vets-service
  namespace: petclinic
  labels:
    app: vets-service
    tier: business
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: vets-service

  # ========================================================================
  # Blue-Green 배포 전략
  # ========================================================================
  strategy:
    blueGreen:
      activeService: vets-service
      previewService: vets-service-preview
      autoPromotionEnabled: false
      scaleDownDelaySeconds: 300
      previewReplicaCount: 2

  template:
    metadata:
      labels:
        app: vets-service
        tier: business
    spec:
      serviceAccountName: vets-service-sa
      containers:
        - name: vets-service
          image: springcommunity/spring-petclinic-vets-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8083
              protocol: TCP

          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 768Mi

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 200
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 20

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 150
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 10

          env:
            - name: JAVA_OPTS
              value: "-Xmx512m -Xms256m"
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes,mysql"
            - name: SERVER_PORT
              value: "8083"
            - name: CONFIG_SERVER_URL
              value: "http://config-server:8888"
            - name: SPRING_APPLICATION_JSON
              value: |
                {
                  "eureka": {
                    "instance": {
                      "preferIpAddress": true
                    },
                    "client": {
                      "serviceUrl": {
                        "defaultZone": "http://discovery-server:8761/eureka/"
                      }
                    }
                  }
                }
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                secretKeyRef:
                  name: petclinic-db-secret
                  key: SPRING_DATASOURCE_URL
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: petclinic-db-secret
                  key: SPRING_DATASOURCE_USERNAME
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: petclinic-db-secret
                  key: SPRING_DATASOURCE_PASSWORD
            - name: SPRING_JPA_HIBERNATE_DDL_AUTO
              value: "update"
            - name: SPRING_JPA_SHOW_SQL
              value: "false"
            - name: SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE
              value: "10"
            - name: SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE
              value: "5"

---
# ============================================================================
# Vets Service (Active) - 운영 트래픽
# ============================================================================
apiVersion: v1
kind: Service
metadata:
  name: vets-service
  namespace: petclinic
  labels:
    app: vets-service
    tier: business
spec:
  selector:
    app: vets-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP
  type: ClusterIP

---
# ============================================================================
# Vets Service Preview - 테스트용 (Blue-Green)
# ============================================================================
apiVersion: v1
kind: Service
metadata:
  name: vets-service-preview
  namespace: petclinic
  labels:
    app: vets-service
    tier: business
    role: preview
spec:
  selector:
    app: vets-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP
  type: ClusterIP
